/**
 * Главное хранилище данных приложения.
 *
 * farms — массив ферм. Каждая ферма будет объектом.
 * events — массив событий (тревоги, предупреждения).
 *
 * Ученик будет наполнять эту структуру по мере выполнения заданий.
 */
const store = {
    farms: [],
    events: [],
};

module.exports = { store };
